'use strict';

/**
 * support-ticket-message router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::support-ticket-message.support-ticket-message');
